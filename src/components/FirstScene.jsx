import React from 'react';

function FirstScene() {
	return (
		<fieldset>
			<legend>first scene</legend>
			<select name="" id="">
				<option value="">first</option>
			</select>
			<button>set</button>
		</fieldset>
	);
}

export default FirstScene;
